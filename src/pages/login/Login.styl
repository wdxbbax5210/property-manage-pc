.login
	position: fixed;
	top:0;
	bottom: 0;
	right: 0;
	left: 0;
	background-color: rgba(0,0,0,0.8);
	z-index: 10;
	.title
		text-align: center;
		font-size: 24px;
		color: #333;
	.content
		position: absolute;
		width: 400px;
		height: 400px;
		left:0;
		right:0
